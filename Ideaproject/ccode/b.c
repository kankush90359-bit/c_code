#include<stdio.h>
int main() {
    int l,b;
    printf("Enter the length and breadth of the rectangle: ");
    scanf("%d %d", &l, &b);
    printf("The area of the rectangle is: %d\n", l*b);
    return 0;
}